datasource db {
  provider = "{PROVIDER}"
  url      = env("CONECTIONSTRING")
}

generator photon {
  provider = "photonjs"
  output   = "../photon"
}

model User {
  id        Int      @id
  name      String
  email     String   @unique
  password  String

  @@map("Users")
}