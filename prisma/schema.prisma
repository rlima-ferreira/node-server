datasource db {
  provider = "{PROVIDER}"
  url      = "{PROVIDER}://{USER}:{PASSWORD}@{HOST}:{PORT}/DB"
}

generator photon {
  provider = "photonjs"
}

model User {
  id        Int      @id
  name      String
  email     String   @unique
  password  String

  @@map("Users")
}